import { TestlibNotlazyModule } from './testlib-notlazy.module';

describe('TestlibNotlazyModule', () => {
    it('should work', () => {
        expect(new TestlibNotlazyModule()).toBeDefined();
    });
});
